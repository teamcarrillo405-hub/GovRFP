import { describe, it } from 'vitest'

describe('ComplianceGapMark', () => {
  it.todo('has name complianceGap')
  it.todo('has requirementId attribute')
  it.todo('renders with data-compliance-gap attribute')
})

describe('stripComplianceMarks', () => {
  it.todo('removes complianceGap marks from Tiptap JSON')
  it.todo('preserves other marks (bold, italic)')
  it.todo('handles document with no marks')
})
