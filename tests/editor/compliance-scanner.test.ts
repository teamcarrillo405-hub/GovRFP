import { describe, it } from 'vitest'

describe('scanCompliance', () => {
  it.todo('returns addressed when 60%+ keywords present')
  it.todo('returns partial when 30-59% keywords present')
  it.todo('returns unaddressed when <30% keywords present')
  it.todo('only checks requirements mapped to given section')
  it.todo('handles empty document gracefully')
})
