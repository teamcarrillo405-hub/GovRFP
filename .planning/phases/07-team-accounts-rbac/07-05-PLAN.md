---
phase: 07-team-accounts-rbac
plan: 05
type: execute
wave: 3
depends_on: [07-03, 07-04]
files_modified: []
autonomous: false

requirements: [TEAM-01, TEAM-02, TEAM-03, TEAM-04, TEAM-05, TEAM-06, TEAM-07]

must_haves:
  truths:
    - "Full flow works: create team, invite, accept, view shared proposal, change role, remove member"
    - "Viewer cannot mutate proposal data via API"
    - "Seat count reflects actual team membership"
    - "Existing user invite shows banner on dashboard"
---

<objective>
Manual verification of the complete team accounts flow.

Purpose: Confirm all TEAM-01 through TEAM-07 requirements work end-to-end in the running application.
Output: Human verification that the full team flow works correctly
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-team-accounts-rbac/07-CONTEXT.md
@.planning/phases/07-team-accounts-rbac/07-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end manual verification of team accounts + RBAC</name>
  <files></files>
  <action>
Present the following verification checklist to the user. This is a checkpoint — no code changes. Wait for user approval.

What was built:
- Database: teams, team_members, team_invites tables with dual RLS on proposals and proposal_sections
- API: 6 route handlers for team CRUD, invite, accept, decline, member management
- Auth: requireProposalRole() server utility enforcing owner > editor > viewer hierarchy
- UI: ShareButton, SharePanel, InviteForm, MemberList, RoleChangeDropdown, RemoveMemberConfirmation
- Pages: /invite/accept, /invite/decline
- Dashboard: shared proposals with purple badge, pending invite banner

How to verify:

Prerequisites: Start dev server with `npm run dev`. Have two test accounts (or use Supabase auth to create them).

Test 1 — Team creation and share (TEAM-01, TEAM-02):
1. Log in as User A (proposal owner)
2. Open an existing proposal
3. Click the "Share" button in the header
4. Enter User B's email, select "Editor" role, click "Send Invite"
5. Verify: SharePanel shows "Invited" badge for User B in the member list
6. Verify: In Supabase dashboard, teams table has a new row with User A as owner, team_members has User A with role=owner, team_invites has User B's email with status=pending

Test 2 — Invite accept (TEAM-03, TEAM-07):
7. Check Supabase email logs (or Inbucket if using local) for invite email to User B
8. If User B is NEW: click invite link -> land on /invite/accept -> "You're in." -> "Go to Proposal"
9. If User B is EXISTING: log in as User B -> dashboard shows yellow pending invite banner -> click "Accept"
10. Verify: team_invites status = 'accepted', team_members has User B with role=editor, teams.seat_count = 2

Test 3 — Team member access (TEAM-06):
11. Log in as User B (editor)
12. Open the shared proposal from dashboard (should show "Shared" purple badge)
13. Verify: proposal loads correctly, editor can view all sections
14. Navigate to editor page -> toolbar is active (editor can edit)

Test 4 — Viewer restriction (TEAM-06, D-16):
15. As User A, change User B's role to "Viewer" via the Share panel
16. Log in as User B -> open the shared proposal
17. Verify: the "Share" button is NOT visible (viewers cannot share)
18. Try calling a mutating API route directly with curl
19. Verify: response is 403 Forbidden

Test 5 — Role change and member removal (TEAM-04, TEAM-05, TEAM-07):
20. As User A, open Share panel, click "Change role" on User B, select "Editor"
21. Verify: badge updates to "Editor"
22. Click remove icon on User B -> confirm "Remove member"
23. Verify: User B disappears from member list, teams.seat_count = 1
24. Log in as User B -> shared proposal no longer appears in dashboard

Test 6 — Invite decline (TEAM-03):
25. As User A, invite a new email (User C)
26. Open the decline link: /invite/decline?invite_id={id}
27. Verify: page shows "Invitation declined" message
28. Verify: team_invites status = 'declined' in DB

Resume signal: Type "approved" if all tests pass, or describe which tests failed with details.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/hcc-proposal-ai && npx vitest run tests/teams/ --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 6 test scenarios pass
    - Viewer cannot mutate proposal data (403 response confirmed)
    - Seat count reflects actual membership after add/remove
  </acceptance_criteria>
  <done>User has approved the complete team accounts flow. All TEAM-01 through TEAM-07 requirements verified manually.</done>
</task>

</tasks>

<verification>
All 6 manual tests pass confirming TEAM-01 through TEAM-07 requirements.
</verification>

<success_criteria>
- Complete invite flow works end-to-end
- Role enforcement prevents viewer mutations at server level
- Seat count tracks membership correctly
- Dashboard shows shared proposals and pending invites
</success_criteria>

<output>
After completion, create `.planning/phases/07-team-accounts-rbac/07-05-SUMMARY.md`
</output>
