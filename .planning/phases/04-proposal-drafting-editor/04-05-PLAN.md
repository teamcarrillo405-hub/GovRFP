---
phase: 04-proposal-drafting-editor
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - src/app/(dashboard)/proposals/[id]/page.tsx
autonomous: false
requirements:
  - DRAFT-01
  - DRAFT-02
  - DRAFT-03
  - DRAFT-04
  - DRAFT-05
  - DRAFT-06
  - EDITOR-01
  - EDITOR-02
  - EDITOR-03
  - EDITOR-04

must_haves:
  truths:
    - "Proposal detail page links to editor when status is 'analyzed'"
    - "Full test suite passes (102+ existing + all new Phase 4 tests)"
    - "Human verified: streaming generates visible content in editor"
    - "Human verified: rich text editing works (bold, heading, table)"
    - "Human verified: auto-save shows timestamp after 30s"
    - "Human verified: compliance panel shows coverage badges"
  artifacts:
    - path: "src/app/(dashboard)/proposals/[id]/page.tsx"
      provides: "Updated proposal detail page with editor link"
      contains: "editor"
  key_links:
    - from: "src/app/(dashboard)/proposals/[id]/page.tsx"
      to: "src/app/(dashboard)/proposals/[id]/editor/page.tsx"
      via: "navigation link"
      pattern: "editor"
---

<objective>
Add navigation from proposal detail page to the editor, run full test suite, and perform human verification of the complete drafting + editing flow.

Purpose: Wire the editor into the existing proposal flow and verify the end-to-end experience works correctly with live Claude API streaming.
Output: Updated proposal page + verified working editor flow
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-proposal-drafting-editor/04-UI-SPEC.md
@.planning/phases/04-proposal-drafting-editor/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editor link to proposal detail page + full test suite verification</name>
  <files>src/app/(dashboard)/proposals/[id]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/proposals/[id]/page.tsx (current proposal detail page — understand existing layout and status display)
    - src/app/(dashboard)/proposals/[id]/analysis/page.tsx (analysis page link pattern)
  </read_first>
  <action>
1. Update `src/app/(dashboard)/proposals/[id]/page.tsx`:
   - Add a "Draft Proposal" link/button that navigates to `/proposals/${id}/editor`
   - Only show this link when proposal status is `'analyzed'` (rfp_analysis exists)
   - Button styling: `inline-flex items-center gap-2 px-4 py-2 bg-blue-700 text-white text-sm font-semibold rounded-md hover:bg-blue-800 transition-colors`
   - Use Next.js `<Link>` component for client-side navigation
   - Position the button prominently — near the top of the page, after the proposal title/status area

2. Run full test suite to verify no regressions:
```bash
npx vitest run --reporter=verbose
```
   - Must pass 102+ existing tests + all new Phase 4 tests
   - Zero failures, zero skipped (except .todo stubs if any remain)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "editor" src/app/(dashboard)/proposals/[id]/page.tsx` returns a match for the editor link
    - `grep "analyzed" src/app/(dashboard)/proposals/[id]/page.tsx` returns a match for status gate
    - `npx vitest run` exits 0 with all tests passing
    - No test regressions from Phase 1-3
  </acceptance_criteria>
  <done>Proposal detail page links to editor for analyzed proposals; full test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of drafting + editor flow</name>
  <files>none</files>
  <action>
Present the human verification checklist to the user. This is a checkpoint — no code changes, only manual testing of the complete Phase 4 editor flow.

What was built:
- 5 section tabs (Executive Summary, Technical Approach, Management Plan, Past Performance, Price Narrative)
- Claude streaming draft generation via "Generate [Section Name]" button
- Rich text editing with headings, bold, italic, underline, lists, tables
- 30-second auto-save with visible timestamp
- Compliance panel showing requirement coverage
- ComplianceGap amber highlighting on unaddressed paragraphs
- Regenerate Section modal with optional instruction input

How to verify:
Prerequisites:
1. Ensure ANTHROPIC_API_KEY is set in .env.local
2. Ensure Supabase is running and migration 00004_proposal_sections.sql has been applied
3. Run npm run dev

Test steps:
1. Navigate to a proposal that has been analyzed (status = 'analyzed') at http://localhost:3000/proposals/{id}
2. Click "Draft Proposal" button — should navigate to editor page
3. Verify 5 section tabs are visible at the top
4. Click "Generate Executive Summary" — verify: button disables, amber border + overlay appears, content streams in, content mentions profile data
5. Edit the generated text — verify bold, italic, underline, headings, bullet lists work
6. Wait 30 seconds — verify "Saved at HH:MM:SS" timestamp appears
7. Switch to "Technical Approach" tab — verify tab switching works
8. Generate Technical Approach — verify it references RFP technical requirements
9. Click "Regenerate Section" — enter "focus more on cybersecurity" — click "Generate New Draft" — verify content shifts
10. Check compliance panel (right sidebar) — verify requirements with addressed/unaddressed badges
11. Look for amber highlighting on paragraphs with unaddressed requirements
12. Test on mobile viewport — verify panel moves below editor

Resume signal: Type "approved" if the editor flow works correctly, or describe specific issues that need fixing.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms streaming generates visible content in the editor
    - User confirms rich text editing works (bold, heading, table, lists)
    - User confirms auto-save shows timestamp after 30 seconds
    - User confirms compliance panel shows coverage badges with correct colors
    - User confirms no emojis visible anywhere in the UI
  </acceptance_criteria>
  <done>Human approved the complete Phase 4 editor flow: streaming, editing, auto-save, compliance panel, regeneration all working end-to-end</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — complete test suite green
2. Human confirmed: streaming generates content
3. Human confirmed: rich text editing works
4. Human confirmed: auto-save shows timestamp
5. Human confirmed: compliance panel shows badges
6. Human confirmed: no emojis in UI
</verification>

<success_criteria>
- Proposal detail page has working link to editor for analyzed proposals
- Full test suite passes (all Phase 1-4 tests)
- Human confirms streaming, editing, auto-save, compliance panel, and regeneration all work end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-proposal-drafting-editor/04-05-SUMMARY.md`
</output>
